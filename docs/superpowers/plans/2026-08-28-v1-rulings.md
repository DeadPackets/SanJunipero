# Owner rulings, 2026-08-28 — the v1 gap audit

Copied verbatim from `~/handoff/cleanup/stage7/RULINGS-2026-08-28.md`, so the repo carries the
decisions the stage-6 and stage-7 lanes were executed against.

## The rulings
- Clip export (WebM/GIF): deferred to v1.x (reverses 2026-08-16 CORE ruling).
- Study pack: replication export only in v1 (route archiving world log + mind DBs + config); rest v1.x.
- Founders: neutral start only; the authored arm is struck; c12 R4's authored clause is void.
- Ops in v1: /api/cost token dashboard, ruling review/revert queue surfaced, pause/speed; regen queue + backup status v1.x.
- Newcomer arrivals: births only in v1.
- Broadcast: finish lower third + ticker.
- Ambience: toponyms painted on the map + institution halos on the bonds graph in v1; audio, PWA, moon/patina/stockpiles/freeze v1.x.
- Deploy + restore drill: last, after everything else; keep files as they are for now.
- Visitor speech: not built, not planned (2026-08-27).
- Art: no palette quantize anywhere; cast at 3 heads; eye is the gate.

## What these supersede

**`2026-08-24-03-genesis-rehearsal-v6.DRAFT.md` is SUPERSEDED and is a record, not a plan.** It was
written against `main` @ `9d76b97`, before the package split (`@sj/gateway` → `@sj/gateway` +
`@sj/town` + `@sj/live`), before the forge chain dropped `quantize`, and before the cast was re-cut
at three heads. Its task numbering does not resolve against this tree.

What replaces it, in order:

| For | Read |
|---|---|
| the architecture moves A–I | `~/handoff/cleanup/arch-digest.md` and the stage-6 lane reports |
| the lane contract | `~/handoff/cleanup/stage6/LANE-RULES.md` |
| what v1 does and does not include | the rulings above |

The C8 delta files beside it (`c8-*-delta.md`) are superseded with it. They describe how the v6
draft differed from its own predecessors, which is history about a document nothing executes.
